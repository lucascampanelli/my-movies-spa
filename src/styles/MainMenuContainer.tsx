import styled from 'styled-components';

export const MainMenuConteiner = styled.nav`
    ul {
        list-style: none;
        padding: 0px;

        li {
            display: inline-block;
            margin-right: 10px;
        }
    }
`